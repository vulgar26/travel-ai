server:
  port: 8081

spring:
  application:
    name: travel-ai-planner

  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/travel_ai?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: "@Zly642465589"

  # Redis配置
  data:
    redis:
      host: 127.0.0.1
      port: 6379

  # Spring AI配置
  ai:
    dashscope:
      api-key: sk-b794530a9b23492591c64eccabc7f891
      chat:
        model: qwen3.5-plus
        options:
          temperature: 0.7
          max-tokens: 2000

# MyBatis Plus配置
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.travel.ai.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.travel.ai: debug